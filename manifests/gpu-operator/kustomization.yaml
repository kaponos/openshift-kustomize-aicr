apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Node Feature Discovery
  - nfd-operatorgroup.yaml
  - nfd-subscription.yaml
  # NVIDIA GPU Operator
  - nvidia-gpu-operatorgroup.yaml
  - nvidia-gpu-subscription.yaml
  - nvidia-gpu-clusterpolicy.yaml
